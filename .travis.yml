language: csharp
mono: none
dotnet: 2.1.101

addons:
  apt:
    packages:
      npm

script:
# restore npm packages
  - cd SpaceY
  - npm install
  - cd ..
# restore nuget packages and run tests
  - dotnet restore
  - dotnet build
  - dotnet test ./SpaceY.Tests/SpaceY.Tests.csproj
